# ISO11820 Modbus 模拟测试故障排查指南

## 问题1: 通讯超时

### 现象
```
程序日志显示: "Modbus通信超时"
```

### 检查清单
- [ ] 虚拟串口对是否正确创建（COM5 ↔ COM6）
- [ ] Modbus Poll 是否连接到正确的端口（COM6）
- [ ] 波特率是否匹配（9600, 8N1）
- [ ] Modbus Poll 是否处于 **Slave 模式**（最常见错误！）
- [ ] 防火墙或杀毒软件是否阻止串口访问

### 解决步骤
1. 打开设备管理器，确认虚拟串口存在
2. 在 Modbus Poll 中：Setup → Slave Definition → 勾选 Enable Slave Simulation
3. 重启 Modbus Poll 和 ISO11820 程序

---

## 问题2: 数据不更新

### 现象
```
Modbus Poll 中看不到程序写入的数据
寄存器值始终为初始值
```

### 检查清单
- [ ] `appsettings.json` 中 `SimulatePidController` 是否为 `false`
- [ ] 程序是否连接到正确的虚拟串口（COM5）
- [ ] Modbus Poll 显示窗口的地址范围是否正确

### 解决步骤
1. 确认配置文件：
```json
{
  "Simulation": {
    "SimulatePidController": false  // 必须为 false
  }
}
```

2. 在 Modbus Poll 中创建正确的显示窗口：
   - 窗口1: Address=0, Quantity=60 (查看写入寄存器)
   - 窗口2: Address=256, Quantity=10 (查看读取寄存器)

---

## 问题3: CRC 校验错误

### 现象
```
Modbus Poll 显示 CRC Error
程序日志显示 ModbusException
```

### 原因
- 数据传输中断
- 通讯参数不匹配
- 虚拟串口驱动问题

### 解决步骤
1. 重新检查通讯参数（9600, 8N1, None）
2. 重启虚拟串口软件
3. 尝试更换虚拟串口软件（com0com → VSPD）

---

## 问题4: 程序无法启动

### 现象
```
程序启动后立即退出
或显示 "PID控制器连接失败"
```

### 检查清单
- [ ] 数据库连接是否正常（SQL Server）
- [ ] COM5 端口是否被其他程序占用
- [ ] 日志文件中是否有详细错误信息

### 解决步骤
1. 检查日志文件：`Logs/iso11820-YYYY-MM-DD.log`
2. 确认 SQL Server 正在运行
3. 使用工具检查串口占用情况：
```cmd
mode COM5
```

---

## 问题5: 温度不升高

### 现象
```
程序显示正在加热，但温度始终为初始值
```

### 原因
- Modbus Poll 中的寄存器值未手动更新
- 未启用自动模拟脚本

### 解决步骤

**方法1: 手动更新（适合测试）**
1. 在 Modbus Poll 中双击寄存器 258
2. 手动输入新的温度值（如 7500 表示 750°C）

**方法2: 使用 Python 脚本（推荐）**
1. 安装依赖：
```cmd
pip install pymodbus pyserial
```

2. 运行模拟脚本：
```cmd
python modbus_slave_simulator.py
```

---

## 问题6: 程序卡在 Preparing 状态

### 现象
```
程序一直显示 "准备中"，无法进入 Ready 状态
```

### 原因
- 温度未达到稳定条件（750°C ± 5°C 持续 10 分钟）
- 温度漂移过大

### 解决步骤
1. 确保寄存器 258 的值在 7450~7550 之间
2. 保持温度稳定至少 10 分钟（600 秒）
3. 温度波动不要超过 ±0.5°C

**快速测试技巧**：
- 修改代码中的稳定时间阈值（仅用于测试）
- 或使用内置仿真模式：
```json
{
  "Simulation": {
    "EnableSimulation": true,
    "SimulatePidController": true
  }
}
```

---

## 调试技巧

### 1. 启用详细日志
修改 `Program.cs` 中的日志级别：
```csharp
.MinimumLevel.Debug()  // 改为 Debug 级别
```

### 2. 监控 Modbus 通信
使用 Modbus Poll 的 **Display → Communication Traffic** 查看实时通信数据

### 3. 串口监控工具
推荐使用：
- **Serial Port Monitor** (Eltima)
- **Free Serial Port Monitor** (HHD Software)

### 4. 测试连接
使用简单的测试程序验证虚拟串口：
```csharp
using System.IO.Ports;

var port = new SerialPort("COM5", 9600, Parity.None, 8, StopBits.One);
port.Open();
Console.WriteLine($"COM5 已打开: {port.IsOpen}");
port.Close();
```

---

## 完整测试流程检查表

### 准备阶段
- [ ] 安装虚拟串口软件（com0com 或 VSPD）
- [ ] 创建虚拟串口对（COM5 ↔ COM6）
- [ ] 安装 Modbus Poll
- [ ] 确认 SQL Server 正在运行

### 配置阶段
- [ ] 修改 `appsettings.json` 中的 `PidPort` 为 COM5
- [ ] 设置 `SimulatePidController` 为 `false`
- [ ] 配置 Modbus Poll 连接参数（COM6, 9600, 8N1）
- [ ] 切换 Modbus Poll 为 Slave 模式

### 测试阶段
- [ ] 启动虚拟串口软件
- [ ] 启动 Modbus Poll
- [ ] 设置初始寄存器值（258=250）
- [ ] 启动 ISO11820 程序
- [ ] 观察日志输出
- [ ] 手动更新温度值或运行 Python 脚本
- [ ] 验证数据交互

### 验证阶段
- [ ] 程序成功连接 PID 控制器
- [ ] 可以看到 Modbus Poll 中的寄存器被写入
- [ ] 程序可以读取到温度值
- [ ] 状态机正常切换（Idle → Preparing → Ready → Recording）

---

## 联系支持

如果以上方法都无法解决问题，请提供以下信息：
1. 日志文件内容（`Logs/iso11820-*.log`）
2. Modbus Poll 截图
3. 虚拟串口配置截图
4. 错误消息的完整堆栈跟踪
