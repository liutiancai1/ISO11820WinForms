# Modbus Poll 详细配置教程

## 📥 第一步：下载安装 Modbus Poll

### 下载地址
- 官网：https://www.modbustools.com/modbus_poll.html
- 试用版可以免费使用（有功能限制）

### 安装
1. 下载 `mbpoll_setup.exe`
2. 双击安装，按默认选项完成安装
3. 启动 Modbus Poll

---

## ⚙️ 第二步：配置串口连接

### 1. 打开连接设置
- 菜单：**Connection → Connect** (或按 `F3`)

### 2. 配置连接参数
```
┌─────────────────────────────────────────────┐
│ Connection Setup                            │
├─────────────────────────────────────────────┤
│ Connection:        [Serial Port]            │
│ Serial Port:       [COM6]          ▼        │
│ Baud:              [9600]           ▼        │
│ Data Bits:         [8]              ▼        │
│ Parity:            [None]           ▼        │
│ Stop Bits:         [1]              ▼        │
│ Mode:              [RTU]            ▼        │
│                                              │
│ Response timeout:  [1000] ms                │
│ Delay after poll:  [0] ms                   │
│                                              │
│         [OK]              [Cancel]           │
└─────────────────────────────────────────────┘
```

**重要参数说明**：
- **Serial Port**: 选择 `COM6`（虚拟串口的另一端）
- **Baud**: `9600`（必须与程序一致）
- **Data Bits**: `8`
- **Parity**: `None`
- **Stop Bits**: `1`
- **Mode**: `RTU`（不是 ASCII！）

### 3. 点击 OK 连接

连接成功后，状态栏会显示：
```
Connected to COM6, 9600, 8, N, 1, RTU
```

---

## 🔄 第三步：切换为 Slave 模式（最重要！）

### ⚠️ 默认情况下 Modbus Poll 是 Master 模式，必须切换为 Slave！

### 1. 打开 Slave 定义
- 菜单：**Setup → Slave Definition** (或按 `F8`)

### 2. 配置 Slave 参数
```
┌─────────────────────────────────────────────┐
│ Slave Definition                            │
├─────────────────────────────────────────────┤
│ ☑ Enable Slave Simulation                   │
│                                              │
│ Slave ID:          [1]                      │
│                                              │
│ Function Codes:                             │
│ ☑ 01 Read Coils                             │
│ ☑ 02 Read Discrete Inputs                   │
│ ☑ 03 Read Holding Registers                 │
│ ☑ 04 Read Input Registers                   │
│ ☑ 05 Write Single Coil                      │
│ ☑ 06 Write Single Register                  │
│ ☑ 15 Write Multiple Coils                   │
│ ☑ 16 Write Multiple Registers               │
│                                              │
│         [OK]              [Cancel]           │
└─────────────────────────────────────────────┘
```

**关键设置**：
- ✅ **勾选** `Enable Slave Simulation`
- **Slave ID**: 设置为 `1`
- **Function Codes**: 全部勾选（特别是 03 和 06）

### 3. 点击 OK 保存

---

## 📊 第四步：创建显示窗口

### 1. 创建新窗口
- 菜单：**File → New** (或按 `Ctrl+N`)

### 2. 配置显示参数
```
┌─────────────────────────────────────────────┐
│ Modbus Poll Setup                           │
├─────────────────────────────────────────────┤
│ Slave ID:          [1]                      │
│ Function:          [03 Read Holding Regs] ▼ │
│ Address:           [0]                      │
│ Quantity:          [300]                    │
│ Scan Rate:         [1000] ms                │
│                                              │
│ Display:           [Signed]         ▼       │
│                                              │
│         [OK]              [Cancel]           │
└─────────────────────────────────────────────┘
```

**参数说明**：
- **Slave ID**: `1`（与 Slave 定义一致）
- **Function**: `03 Read Holding Registers`
- **Address**: `0`（起始地址）
- **Quantity**: `300`（读取 300 个寄存器，覆盖 0~299）
- **Scan Rate**: `1000` ms（每秒刷新一次）
- **Display**: `Signed` 或 `Unsigned`（根据需要）

### 3. 点击 OK 创建窗口

---

## 📝 第五步：设置初始寄存器值

### 1. 找到关键寄存器

在显示窗口中，找到以下地址：

```
┌──────┬──────────┬──────────┬──────────┐
│ 地址 │ 功能     │ 初始值   │ 说明     │
├──────┼──────────┼──────────┼──────────┤
│ 0    │ 目标温度 │ 0        │ 程序会写 │
│ 2    │ 手动输出 │ 0        │ 程序会写 │
│ 56   │ 控制模式 │ 0        │ 程序会写 │
│ 257  │ PID输出  │ 0        │ 你需设置 │
│ 258  │ 当前温度 │ 250      │ 你需设置 │
└──────┴──────────┴──────────┴──────────┘
```

### 2. 修改寄存器值

**方法1：双击修改**
1. 找到地址 `258` 的行
2. 双击 "Value" 列
3. 输入 `250`（表示 25.0°C）
4. 按 `Enter` 确认

**方法2：右键菜单**
1. 右键点击地址 `258` 的行
2. 选择 "Write Single Register"
3. 输入值 `250`
4. 点击 OK

### 3. 验证修改

修改后，该行应该显示：
```
Address: 258
Value: 250
```

---

## 🎮 第六步：模拟温度变化

### 手动模拟升温过程

按照以下时间表修改寄存器 258 的值：

```
┌──────┬──────────┬──────────┬────────────────┐
│ 时间 │ 温度(°C) │ 寄存器值 │ 操作           │
├──────┼──────────┼──────────┼────────────────┤
│ 0s   │ 25       │ 250      │ 初始值         │
│ 30s  │ 200      │ 2000     │ 双击修改       │
│ 60s  │ 400      │ 4000     │ 双击修改       │
│ 90s  │ 600      │ 6000     │ 双击修改       │
│ 120s │ 750      │ 7500     │ 双击修改       │
│ 150s+│ 750±5    │ 7450~7550│ 随机小幅波动   │
└──────┴──────────┴──────────┴────────────────┘
```

### 保持温度稳定

达到 750°C 后，需要保持稳定：
1. 每隔几秒修改一次寄存器 258
2. 值在 `7450` ~ `7550` 之间随机变化
3. 模拟真实的温度波动

---

## 👀 第七步：观察数据交互

### 1. 观察程序写入的寄存器

程序启动后，应该看到以下寄存器被写入：

**地址 0（目标温度）**：
```
Address: 0
Value: 7500  ← 程序写入（750.0°C）
```

**地址 2（手动输出）**：
```
Address: 2
Value: 0 → 7680 → 12800 → ...  ← 根据温度阶段变化
```

**地址 56（控制模式）**：
```
Address: 56
Value: 3  ← 手动模式
或
Value: 4  ← PID 模式
```

### 2. 查看通信流量

- 菜单：**Display → Communication Traffic** (或按 `F2`)
- 可以看到所有 Modbus 报文的十六进制数据

示例：
```
TX: 01 06 00 00 1D 4C XX XX  ← 写入目标温度 7500
RX: 01 06 00 00 1D 4C XX XX  ← 从站响应

TX: 01 03 01 02 00 01 XX XX  ← 读取当前温度（地址258）
RX: 01 03 02 00 FA XX XX     ← 返回值 250（25.0°C）
```

### 3. 监控状态栏

状态栏会显示：
- 连接状态
- 通信错误计数
- 最后一次通信时间

---

## 🔍 第八步：调试技巧

### 技巧1：使用颜色标记

右键点击寄存器行 → "Properties" → 设置颜色：
- 🟢 绿色：程序会读取的寄存器（257, 258）
- 🔵 蓝色：程序会写入的寄存器（0, 2, 56）

### 技巧2：添加注释

右键点击寄存器行 → "Properties" → "Comment"：
```
Address 0:   目标温度 (SV)
Address 2:   手动输出 (MV)
Address 56:  控制模式
Address 257: PID输出
Address 258: 当前温度 (PV)
```

### 技巧3：保存配置

- 菜单：**File → Save** (或按 `Ctrl+S`)
- 保存为 `.mbp` 文件，下次直接打开

### 技巧4：使用多个窗口

可以创建多个显示窗口，分别监控不同的地址范围：

**窗口1：写入寄存器（0~60）**
```
Address: 0
Quantity: 60
```

**窗口2：读取寄存器（256~270）**
```
Address: 256
Quantity: 15
```

---

## ✅ 验证测试成功

### 成功标志

1. **连接成功**
   - 状态栏显示 "Connected to COM6"
   - 无通信错误

2. **Slave 模式已启用**
   - Setup → Slave Definition 中已勾选
   - 可以看到程序写入的数据

3. **数据交互正常**
   - 地址 0 被写入 7500
   - 地址 2 的值会变化
   - 地址 56 在 3 和 4 之间切换

4. **程序正常运行**
   - 程序界面显示实时温度
   - 温度值与寄存器 258 一致
   - 无 "Modbus 通信超时" 错误

---

## ❌ 常见错误排查

### 错误1：程序显示 "Modbus 通信超时"

**原因**：Modbus Poll 未启用 Slave 模式

**解决**：
1. Setup → Slave Definition (F8)
2. 勾选 "Enable Slave Simulation"
3. 点击 OK

### 错误2：寄存器值不变化

**原因**：连接参数不匹配

**检查**：
- 端口：COM6
- 波特率：9600
- 模式：RTU（不是 ASCII）
- Slave ID：1

### 错误3：CRC 错误

**原因**：通信参数错误

**解决**：
- 确认数据位：8
- 确认校验位：None
- 确认停止位：1
- 重启 Modbus Poll

### 错误4：无法连接 COM6

**原因**：虚拟串口未创建或被占用

**解决**：
1. 打开设备管理器，确认 COM6 存在
2. 关闭其他可能占用 COM6 的程序
3. 重新创建虚拟串口对

---

## 📚 快速参考卡

### 关键快捷键
```
F3  - 连接/断开
F8  - Slave 定义
F2  - 通信流量
Ctrl+N - 新建窗口
Ctrl+S - 保存配置
```

### 关键寄存器
```
0   - 目标温度 (写)
2   - 手动输出 (写)
56  - 控制模式 (写)
257 - PID输出 (读)
258 - 当前温度 (读)
```

### 温度换算
```
寄存器值 = 温度 × 10
例如：
  25.0°C  = 250
  750.0°C = 7500
  745.2°C = 7452
```

---

## 🎯 下一步

配置完成后：
1. 启动 ISO11820 程序
2. 观察 Modbus Poll 中的数据变化
3. 手动修改寄存器 258 模拟温度变化
4. 验证程序状态机正常切换

如果需要自动化模拟，可以使用 `modbus_slave_simulator.py` 脚本！
