# 虚拟串口配置步骤

## 方法1：使用 com0com（推荐，免费）

### 下载安装
1. 下载地址：https://sourceforge.net/projects/com0com/
2. 下载 `com0com-2.2.2.0-x64-fre-signed.zip`
3. 解压后运行 `setup_com0com_W7_x64_signed.exe`

### 创建虚拟串口对
1. 安装完成后，打开 "开始菜单" → 搜索 "Setup Command Prompt"
2. 右键 "以管理员身份运行"
3. 输入以下命令：

```cmd
install PortName=COM5 PortName=COM6
```

4. 验证是否创建成功：
```cmd
list
```

应该看到：
```
CNCA0 PortName=COM5
CNCB0 PortName=COM6
```

### 查看虚拟串口
- 打开 "设备管理器"
- 展开 "端口 (COM 和 LPT)"
- 应该看到：
  - COM5 (com0com - serial port emulator)
  - COM6 (com0com - serial port emulator)

---

## 方法2：使用 Virtual Serial Port Driver (VSPD)

### 下载安装
1. 下载地址：https://www.eltima.com/vspd/
2. 安装 VSPD（有试用版）

### 创建虚拟串口对
1. 打开 VSPD 管理器
2. 点击 "Add Pair"
3. 选择 COM5 和 COM6
4. 点击 "Add Pair" 按钮

---

## 验证虚拟串口

### 使用 PowerShell 验证
```powershell
[System.IO.Ports.SerialPort]::GetPortNames()
```

应该看到 COM5 和 COM6 在列表中。

### 连接示意图
```
┌──────────────────────┐         ┌──────────────────────┐
│  ISO11820 程序       │         │  Modbus Poll         │
│  (Modbus Master)     │         │  (Modbus Slave)      │
│                      │         │                      │
│  连接: COM5          ├─────────┤  连接: COM6          │
│                      │  虚拟   │                      │
│  发送命令 ──────────>│  串口对 │──────────> 接收命令  │
│  <────────── 接收数据│         │  发送数据 <──────────│
└──────────────────────┘         └──────────────────────┘
```

---

## 常见问题

### 问题1：端口已被占用
如果 COM5 或 COM6 已存在，可以使用其他端口号：
```cmd
install PortName=COM10 PortName=COM11
```
然后修改 `appsettings.json` 中的 `PidPort` 为 COM10。

### 问题2：无法创建虚拟串口
- 确保以管理员身份运行
- 检查是否有其他虚拟串口软件冲突
- 重启计算机后重试

### 问题3：设备管理器中看不到端口
- 重新安装 com0com
- 检查驱动是否正确安装
- 查看设备管理器中是否有黄色感叹号
