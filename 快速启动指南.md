# ISO11820 Modbus 模拟测试 - 快速启动指南

## 🚀 5分钟快速上手

### 步骤1: 安装虚拟串口（2分钟）

**推荐：com0com（免费开源）**

1. 下载：https://sourceforge.net/projects/com0com/
2. 安装后运行 `Setup Command Prompt`
3. 执行命令创建虚拟串口对：
```cmd
install PortName=COM5 PortName=COM6
```

4. 验证：
```cmd
list
```
应该看到：`COM5 <-> COM6`

---

### 步骤2: 配置 Modbus Poll（1分钟）

1. 打开 Modbus Poll
2. **Connection → Connect**：
   - Port: `COM6`
   - Baud: `9600`
   - Mode: `RTU`
   - 点击 OK

3. **Setup → Slave Definition (F8)**：
   - ✅ 勾选 `Enable Slave Simulation`
   - Slave ID: `1`
   - 点击 OK

4. 创建显示窗口（**File → New**）：
   - Function: `03 Read Holding Registers`
   - Address: `0`
   - Quantity: `300`
   - 点击 OK

5. 设置初始温度：
   - 找到地址 `258` 的行
   - 双击数值列
   - 输入 `250`（表示 25.0°C）
   - 回车确认

---

### 步骤3: 修改项目配置（30秒）

编辑 `appsettings.json`：

```json
{
  "Hardware": {
    "PidPort": "COM5"  // ← 改这里
  },
  "Simulation": {
    "EnableSimulation": false,
    "SimulateSensors": true,
    "SimulatePidController": false  // ← 必须是 false
  }
}
```

---

### 步骤4: 启动程序（30秒）

1. 在 Visual Studio 中按 `F5` 运行
2. 登录系统
3. 观察日志输出：
```
[INFO] 正在连接PID控制器，端口: COM5
[INFO] PID控制器连接成功，开始初始化...
[INFO] PID控制器初始化完成
```

---

### 步骤5: 模拟温度变化（1分钟）

**方法A: 手动模拟（适合快速测试）**

在 Modbus Poll 中手动修改寄存器 258 的值：

| 时间 | 温度 | 寄存器258值 | 说明 |
|------|------|-------------|------|
| 0s | 25°C | 250 | 初始温度 |
| 30s | 300°C | 3000 | 升温中 |
| 60s | 600°C | 6000 | 继续升温 |
| 90s | 750°C | 7500 | 达到目标温度 |
| 120s+ | 750°C | 7450~7550 | 保持稳定（随机波动） |

**方法B: 自动模拟（推荐）**

1. 安装 Python 依赖：
```cmd
pip install pymodbus pyserial
```

2. 关闭 Modbus Poll

3. 运行模拟脚本：
```cmd
python modbus_slave_simulator.py
```

4. 重新启动 ISO11820 程序

---

## 📊 观察数据交互

### 在 Modbus Poll 中观察

**程序会写入的寄存器**：

| 地址 | 名称 | 预期值 | 说明 |
|------|------|--------|------|
| 0 | 目标温度 | 7500 | 程序启动时写入 |
| 2 | 手动输出 | 0~25600 | 加热时会变化 |
| 56 | 控制模式 | 3 或 4 | 3=手动, 4=PID |

**程序会读取的寄存器**：

| 地址 | 名称 | 你需要设置 | 说明 |
|------|------|------------|------|
| 257 | PID输出 | 0~25600 | 模拟PID输出功率 |
| 258 | 当前温度 | 250~7500 | 模拟实时温度 |

### 在程序界面观察

1. **主界面** 应显示：
   - 炉内温度1: 实时更新
   - 炉内温度2: 实时更新
   - 控制器状态: Idle → Preparing → Ready

2. **日志窗口** 应显示：
```
[DEBUG] Modbus写入: 地址=0x0000, 值=0x1D4C
[DEBUG] Modbus写入成功
[DEBUG] Modbus读取: 地址=0x0102
[DEBUG] Modbus读取成功: 值=0x00FA (250)
```

---

## ✅ 验证测试成功

### 成功标志

- [ ] 程序日志显示 "PID控制器连接成功"
- [ ] Modbus Poll 中可以看到寄存器 0, 2, 56 被写入
- [ ] 程序界面显示实时温度（与寄存器 258 的值一致）
- [ ] 点击"开始加热"后，寄存器 2 的值增加
- [ ] 温度达到 750°C 后，状态变为 "Ready"

### 常见错误

❌ **"Modbus通信超时"**
→ 检查 Modbus Poll 是否启用了 Slave 模式

❌ **"PID控制器连接失败"**
→ 检查虚拟串口是否正确创建

❌ **温度不变化**
→ 手动修改 Modbus Poll 中寄存器 258 的值

---

## 🎯 完整测试场景

### 场景1: 升温测试（约3分钟）

1. 启动程序，点击"开始加热"
2. 在 Modbus Poll 中每10秒增加寄存器 258 的值：
   - 250 → 1000 → 2000 → 3000 → ... → 7500
3. 观察程序界面温度曲线
4. 观察寄存器 2（手动输出）的变化

### 场景2: 稳定测试（约10分钟）

1. 将寄存器 258 设置为 7500
2. 保持10分钟不变（或小幅波动 ±5）
3. 观察程序状态从 Preparing → Ready
4. 此时满足试验开始条件

### 场景3: 完整试验（约20分钟）

1. 完成场景2后，点击"开始试验"
2. 程序切换为手动模式（寄存器 56 = 3）
3. 保持温度稳定
4. 等待试验完成（约10分钟）
5. 程序自动保存数据并生成报告

---

## 🔧 高级技巧

### 技巧1: 加速测试

修改代码中的时间阈值（仅用于测试）：

```csharp
// Core/TestMaster.cs
// 将 600 秒改为 60 秒
if (y1Data10Min.Count >= 60)  // 原来是 600
{
    // ...
}
```

### 技巧2: 使用内置仿真

如果不想配置 Modbus Poll，可以使用内置仿真：

```json
{
  "Simulation": {
    "EnableSimulation": true,
    "SimulatePidController": true,
    "HeatingRatePerSecond": 10.0  // 加快升温速度
  }
}
```

### 技巧3: 监控通信数据

在 Modbus Poll 中：
- **Display → Communication Traffic**
- 可以看到所有 Modbus 报文的十六进制数据

---

## 📞 需要帮助？

查看详细文档：
- `md/Modbus_Poll模拟配置指南.md` - 完整配置说明
- `模拟测试故障排查指南.md` - 问题排查
- `modbus_slave_simulator.py` - 自动化模拟脚本

检查日志文件：
- `Logs/iso11820-YYYY-MM-DD.log`

---

## 🎉 下一步

测试成功后，你可以：
1. 连接真实的 PID 温控器硬件
2. 集成其他传感器（表面温度、中心温度）
3. 配置火焰检测摄像头
4. 自定义试验报告模板

祝测试顺利！🚀
